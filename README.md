# nektimer

A simple, terminal-native countdown timer for deep-work sessions.

### Features
- Sends a `SwayNC` notification when the timer expires.
- Create a text .csv file to store the logs.

### How to use
- clone the repo
- make a csv file named: "timer_log.csv"
- chmod +x timer.sh
- ./timer.sh 
